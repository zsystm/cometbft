prometheus = true

# 100 * 10 * 1 Mb tx per second to validator04
load_tx_connections = 10
load_tx_batch_size = 10
load_tx_size_bytes = 1048574 # 1Mb - 2 bytes for the header
load_max_txs = 20

# to allow sending big txs via websockets
rpc_max_header_bytes = 2097152
rpc_max_body_bytes = 2097152

# nodes to send txs to
[node.validator01]
