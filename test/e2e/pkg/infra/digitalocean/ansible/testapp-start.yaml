---
- name: start test app
  hosts: validators,ephemeral
  gather_facts: false
  tasks:
    - name: operate on the systemd-unit
      ansible.builtin.systemd:
        name: testappd
        state: started
        enabled: yes
